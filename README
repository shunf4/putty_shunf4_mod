

{
                        char abc[300];sprintf(abc, "eee char %ld, len=%d", (*text), len);
                        OutputDebugStringA(abc);
                    } 

taskkill /f /im pterm.exe ; cmake --build . --config Release


PuTTY source code README
========================

This is the README for the source code of PuTTY, a free Windows and
Unix Telnet and SSH client.

PuTTY is built using CMake <https://cmake.org/>. To compile in the
simplest way (on any of Linux, Windows or Mac), the general method is
to run these commands in the source directory:

  cmake .
  cmake --build . --config Release

These commands will expect to find a usable compile toolchain on your
path. So if you're building on Windows with MSVC, you'll need to make
sure that the MSVC compiler (cl.exe) is on your path, by running one
of the 'vcvars32.bat' setup scripts provided with the tools. Then the
cmake commands above should work.

To install in the simplest way on Linux or Mac:

  cmake --build . --target install

On Unix, pterm would like to be setuid or setgid, as appropriate, to
permit it to write records of user logins to /var/run/utmp and
/var/log/wtmp. (Of course it will not use this privilege for
anything else, and in particular it will drop all privileges before
starting up complex subsystems like GTK.) The cmake install step
doesn't attempt to add these privileges, so if you want user login
recording to work, you should manually ch{own,grp} and chmod the
pterm binary yourself after installation. If you don't do this,
pterm will still work, but not update the user login databases.

pterm (Windows) command-line usage
-----------------------------------

On Windows, pterm.exe supports the following command-line options:

  pterm [OPTIONS] [-e COMMAND [ARGS...]]

Options:

  -h, --help, -?, /?         Show help message
  -load SESSION              Load a saved session by name
  @SESSION                   Load a saved session (alternative syntax)
  -o KEY=VALUE               Override a configuration setting
  -sessionlog FILE           Log session output to FILE
  -e COMMAND [ARGS...]       Run COMMAND instead of the default shell

### The -o override option

The `-o KEY=VALUE` option allows overriding any terminal configuration
setting from the command line.  The key names match the PascalCase names
used in the Windows registry under
`HKEY_CURRENT_USER\Software\SimonTatham\PuTTY\Sessions\`.

Common overrides:

  Window size and position:
    -o TermWidth=80            Terminal width in columns (default: 80)
    -o TermHeight=24           Terminal height in rows (default: 24)
    -o 'WinTitle=My Title"     Set the initial window title
    -o "StartupWindowPos=@active:center"
                                Center on the monitor with mouse cursor
    -o "StartupWindowPos=@screen:100,200"
                                Position in virtual desktop coordinates
    -o "StartupWindowPos=0:0,0"
                                Top-left of the first monitor (by index)
    -o "StartupWindowPos=\\\\.\\DISPLAY2:center"
                                By monitor device name

  StartupWindowPos format: `<monitor>:<position>`

    `<monitor>` can be:
      @active   = monitor containing the mouse cursor
      @screen   = virtual desktop (all monitors combined)
      <number>  = monitor index (0, 1, 2, ...)
      <name>    = device name, e.g. `\\.\DISPLAY1`

    `<position>` can be:
      x,y       = pixel coordinates relative to the monitor work area
      center    = center the window in the monitor

  Scrollback:
    -o ScrollbackLines=2000    Number of scrollback lines (default: 2000)
    -o ScrollBar=true          Show/hide the scrollbar (true/false)

  Font:
    -o "Font=Consolas,14"      Font name and height
    -o "Font=Courier New,12"   Font name with spaces must be quoted
    Font value format: FontName[,Height[,Bold[,Charset]]]

  Colours:
    -o Colour0=255,255,255     Default Foreground (R,G,B, 0-255)
    -o Colour2=0,0,0           Default Background (R,G,B, 0-255)
    -o Colour1=255,255,255     Default Bold Foreground (R,G,B, 0-255)

  Copy & paste:
    -o CtrlShiftCV=explicit    Enable Ctrl+Shift+C/V copy/paste
                               Values: none, implicit, explicit,
                                       custom:<clipboard-name>
    -o MousePaste=implicit     Mouse paste mode
    -o MouseAutocopy=true      Auto-copy selected text

  Zoom (in windowed mode zoom resizes the window; in maximized or
  fullscreen mode zoom adjusts terminal columns/rows to fill the screen):
    Ctrl+Scroll Up/Down        Zoom font size in/out
    Ctrl+Shift++               Zoom in  (Ctrl+Shift+= on US keyboard)
    Ctrl+Shift+-               Zoom out (Ctrl+Shift+_ on US keyboard)
    Ctrl+0                     Reset font to default size

  Bell / indication:
    -o BeepInd=0               Bell indication mode:
                                 0 / B_IND_DISABLED = no indication
                                 1 / B_IND_FLASH    = flash window
                                 2 / B_IND_STEADY   = steady indication
    -o ScrollOnKey=true        Scroll to bottom on keypress
    -o ScrollOnDisp=true       Scroll to bottom on new output
    -o ScrollBarFullScreen=false
                               Show scrollbar in fullscreen mode

  Windows console behaviour (pterm-specific):
    -o MsWinConsoleBehaviourOnStart=attach
                               Console behaviour on startup:
                                 attach    = AttachConsole(ATTACH_PARENT_PROCESS)
                                             (default)
                                 alloc     = AllocConsole()
                                 allocHide = not implemented
                                 none      = do not attach or allocate

  ConPTY flags (pterm-specific):
    -o "MsWinConptyFlags=PSEUDOCONSOLE_INHERIT_CURSOR|PSEUDOCONSOLE_WIN32_INPUT_MODE"
                               Flags passed to CreatePseudoConsole.
                               Use flag names with | or a hex/dec integer.
                               Default (empty): all flags (0x7).
                               Available flags:
                                 PSEUDOCONSOLE_INHERIT_CURSOR   (0x1)
                                 PSEUDOCONSOLE_RESIZE_QUIRK     (0x2)
                                 PSEUDOCONSOLE_WIN32_INPUT_MODE (0x4)

  -o MsWinWaitBeforeConsoleBehaviourMsec=2000
  -o MsWinWaitAfterConsoleBehaviourMsec=2000

Value types:
  - Boolean: true/false, yes/no, or 1/0
  - Integer: decimal numbers
  - Enum int: integer or enum constant name (e.g. B_IND_FLASH)
  - String: used as-is (quote the whole -o argument if it contains spaces)
  - Font:    FontName[,Height[,Bold[,Charset]]]
  - Colour:  R,G,B (three integers 0-255)

For a complete list of configuration keys and their meanings, see the
registry values in any saved session, or configure settings in PuTTY.exe
and reuse them via -load.

Examples:
  pterm -o TermWidth=120 -o TermHeight=36 -o ScrollbackLines=10000
  pterm -o "Font=Consolas,14" -o CtrlShiftCV=explicit
  pterm -o WinTitle=Server -o "StartupWindowPos=@active:center"
  pterm -o Colour0=255,255,255 -o Colour2=30,30,30  (light on dark)
  pterm -load "Default Settings" -o TermWidth=100 -e cmd
  pterm -o MsWinConsoleBehaviourOnStart=none -o MsWinConptyFlags=0x5 -e wsl

Documentation (in various formats including Windows Help and Unix
`man' pages) is built from the Halibut (`.but') files in the `doc'
subdirectory. If you aren't using one of our source snapshots,
you'll need to do this yourself. Halibut can be found at
<https://www.chiark.greenend.org.uk/~sgtatham/halibut/>.

The PuTTY home web site is

    https://www.chiark.greenend.org.uk/~sgtatham/putty/

If you want to send bug reports or feature requests, please read the
Feedback section of the web site before doing so. Sending one-line
reports saying `it doesn't work' will waste your time as much as
ours.

See the file LICENCE for the licence conditions.


## Taskbar Shift+Click / Middle-Click Setup

pterm is a portable executable without an installer, so Windows has no
Start Menu shortcut to use as its "launch entry".  This means
**Shift+Click** and **middle-click** on the taskbar button will not
spawn a new instance by default.

The script `create-pterm-shortcut.ps1` creates a shortcut (`.lnk`) with
the correct `AppUserModelID` that matches pterm's internal ID.  Once
this shortcut is in the Start Menu, Windows can find it and launch a
new instance when you Shift+Click or middle-click the taskbar icon.

### Quick start

```powershell
# Create shortcut in current directory AND install to Start Menu:
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1 -InstallStartMenu
```

After running the above, restart pterm.  Shift+Click or middle-click the
taskbar icon -- a new instance will launch.

### All options

```powershell
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1 `
    [-ShortcutDir    <dir>]     `  # output directory (default: .)
    [-ShortcutName   <name>]    `  # .lnk filename   (default: pterm-change-target-and-copy-to-start-menu-programs.lnk)
    [-TargetPath     <exe>]     `  # path to pterm    (default: .\Release\pterm.exe)
    [-Arguments      <args>]    `  # command-line args (default: empty. Use \" to escape double quotes in win process command line.)
    [-AppUserModelId <id>]      `  # must match pterm.c (default: SimonTatham.Pterm.shunf4-mod)
    [-InstallStartMenu]            # also copy to Start Menu
```

### Examples

```powershell
# Default settings, install to Start Menu
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1 -InstallStartMenu

# With custom font and window size
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1 `
    -Arguments '-o "Font=Consolas,14" -o TermWidth=120 -o TermHeight=36' `
    -InstallStartMenu

# Launch WSL by default
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1 `
    -Arguments '-e wsl' `
    -InstallStartMenu

# Create shortcut in current directory only (manual install later)
powershell -ExecutionPolicy Bypass -File create-pterm-shortcut.ps1
```

### Notes

- `-Arguments` sets the **default** launch parameters for the new
  instance spawned by Shift+Click.  The currently running instance is
  unaffected.  If you want every new instance to use the same arguments
  as the current one, put those arguments in `-Arguments`.

- To remove the Start Menu entry, delete the `.lnk` file from
  `%APPDATA%\Microsoft\Windows\Start Menu\Programs\`.

- The `AppUserModelId` must match the value returned by
  `get_app_user_model_id()` in `windows/pterm.c`.  If you change it
  there, re-run the script with the matching `-AppUserModelId`.
